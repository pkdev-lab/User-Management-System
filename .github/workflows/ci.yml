name: Test Workflow

on:
    push:
        branches:
            - dev

jobs:
    test:
        runs-on: ubuntu-latest

        steps:

            - name: Run Command
              run: echo "Hello DevOps!"